apiVersion: v1
kind: Service
metadata:
  name: nodeapp-service
  namespace: proj-no-secrets
spec:
  selector:
    app: nodeapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 3000
  type: NodePort
